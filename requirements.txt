# The whole kit. Six direct dependencies.
pydantic>=2.5          # typed contracts at every boundary
httpx>=0.24            # model calls
sqlite-vec>=0.1.9      # vector search inside the run database
fastapi>=0.103         # the expert callback form
uvicorn>=0.22          # serves it
# dev
pytest>=7.0
